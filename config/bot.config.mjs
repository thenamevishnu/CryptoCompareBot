export const settings = {
    admin: {
        id: process.env.ADMIN_ID,
        name: process.env.ADMIN_NAME,
        username: process.env.ADMIN_USERNAME,
    },
    bot: {
        name: process.env.BOT_NAME,
        username: process.env.BOT_USERNAME,
    },
    chats: {
        group: process.env.GROUP_USERNAME,
    }
}